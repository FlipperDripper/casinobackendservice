import {Injectable} from "@nestjs/common";

@Injectable()
class FinanceService {
    deposit(userId: number, sum: number, description?: string){

    }
    withdraw(userId: number, sum: number, description?: string){

    }
}